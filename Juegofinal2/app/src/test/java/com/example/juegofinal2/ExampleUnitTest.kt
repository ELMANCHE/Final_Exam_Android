package com.example.juegofinal2

import org.junit.Test
import org.junit.Assert.*

/**
 * Ejemplo de prueba unitaria local, que se ejecutará en la máquina de desarrollo (host).
 *
 * Consulta la [documentación sobre pruebas](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {

    // Método de prueba para verificar la suma
    @Test
    fun addition_isCorrect() {
        // Verifica que la suma de 2 + 2 sea igual a 4
        assertEquals(4, 2 + 2)
    }
}
